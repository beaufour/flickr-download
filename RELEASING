# Releases

To release a new version of the tool use the `./release.sh` script.

## PyPI Authentication

To configure auth for PyPI for the first time do:

    uv publish --token my-token

Or set the `UV_PUBLISH_TOKEN` environment variable.
